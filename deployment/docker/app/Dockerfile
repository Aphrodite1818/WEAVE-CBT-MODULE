# Phase 1 WEAVE application image Dockerfile.
# Purpose: build the frontend assets, compile the API and worker with Nuitka,
# then assemble one source-free application image that can run either role.
# Implementation intentionally deferred until the compile/build steps are designed and verified.
