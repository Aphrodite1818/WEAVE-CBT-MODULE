# Phase 1 frontend Dockerfile placeholder.
# Purpose: build both Vite applications and package only their production static output.
# Implementation intentionally deferred until we design the build and runtime stages explicitly.
