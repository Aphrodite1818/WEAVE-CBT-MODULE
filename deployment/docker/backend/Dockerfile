# Phase 1 production backend Dockerfile.
# Purpose: compile the API and worker with Nuitka in a builder stage, then package only compiled runtime artifacts.
# Source correctness is validated by CI; this is the only backend Docker path intended for release.
# Implementation intentionally deferred until the Nuitka build is designed and verified.
