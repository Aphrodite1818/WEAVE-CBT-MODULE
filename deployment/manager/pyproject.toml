[build-system]
requires = ["setuptools>=75", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "weave-cbt-manager"
version = "0.1.0"
description = "WEAVE CBT deployment manager"
requires-python = ">=3.11"

# Planned runtime tools for implementation phase:
# - PySide6: desktop GUI
# - Typer + Rich: CLI
# - httpx: HTTP/update/health interactions
# - psutil: host prerequisite checks
# - Nuitka: compiled distribution
